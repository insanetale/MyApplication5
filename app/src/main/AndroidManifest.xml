<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.psystudio.myapplication">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_laucherbycle"
        android:label="자찬가(성동구편)"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".LenderActivity" />
        <activity android:name=".SellActivity" />
        <activity android:name=".SelecetActivity" />
        <activity android:name=".GuardsActivity" />
        <activity android:name=".YoutubeActivity">

        </activity>
        <activity android:name=".LoginActivity"></activity>
    </application>

</manifest>    <!--&lt;!&ndash;매일 처음 인트로 화면으로 돌리는 방법&#45;&#45;13-17번째 쥴>-->
